# 초원 슬라임 Animation 배치 규칙

`01_GrassSlime_Walk.png`를 Unity에서 import한 뒤 생성하는 Animation Clip과 Animator Controller를 이 폴더에 저장한다.

- 실제 Sprite 프레임 구성이 확인된 뒤 Clip과 Controller를 만든다.
- 빈 Animation 또는 Placeholder Controller를 미리 만들지 않는다.
- Unity의 정상 Asset 생성·import 과정에서 `.meta`를 생성한다.
